{
  "name": "ai-cricket-coach-root",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm start",
    "install-all": "npm install --prefix frontend && npm install --prefix backend"
  }
}
